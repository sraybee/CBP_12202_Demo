apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: cbp-12202-action-combinations
on:
  workflow_dispatch:
jobs:
  checkout-combos:
    steps:
      - name: Both ID and Name
        id: checkout-both
        uses: cloudbees-io/checkout@v1

      - name: Name Only
        uses: cloudbees-io/checkout@v1

      - id: ID Only
        uses: cloudbees-io/checkout@v1

      - uses: cloudbees-io/checkout@v1

      - uses: ./actions/cleanup-test

      - name: Hello World Fromm Alpine
        uses: docker://alpine:3.21
        shell: sh
        run: |
          echo "Hello World"
